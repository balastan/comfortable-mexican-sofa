.page-header
  %h2= t('.title')

= comfy_form_for @photo, :as => :gallery_photo, :url => { :action => :update }, :enctype => "multipart/form-data", :html => { :multipart => true } do |form|
  = render form